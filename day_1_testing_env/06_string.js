
const ename = new String("Yashas");

console.log(ename)

console.log(ename.toLowerCase());

console.log(`Hello ${ename} and lower case is ${ename.toLowerCase()} and length = ${ename.length}`);

// to see all the method/prototype of a datatype or object 
// go to browser console and enter that object 
// ex: const ename = new String("Test") then enter ename, now we can see all the method/prototype. 

